provider "aws" {
  region = var.aws_reg
}

#terraform {
  #required_providers {
    #template = {
      #source  = "hashicorp/template"
      #version = "~> 2.1.2"
    #}
          #ibm = {
      #source = "ibm-cloud/ibm"
    #}
  #}
#}
